<ul>
  <% @questions.order(created_at: :desc).each do |question| %>
    <li><%= link_to question.title, question_path(question)%></li>
  <% end %>
</ul>
<%= link_to "Ask a Question", new_question_path, class: "button right" %>
